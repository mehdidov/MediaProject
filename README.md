# Media Project – Django API

Small school project with:

- Django + Django REST Framework
- Docker + docker-compose
- Swagger documentation (drf-spectacular)
- Postman collection

## Run with Docker

```bash
docker compose up --build
